# [tox].envlist generated by update_config.py using pre-commit, toml reformatted
[tox]
envlist = {{{py_versions}}}-plugins_{{{plugin_name}}}-bodhilib_{{{core_versions}}}

[testenv]
deps = 
    poetry
allowlist_externals = 
    poetry
commands = 
    poetry run pytest tests